# Requisitos del servidor (Ubuntu) para desplegar 
# Este archivo lista las dependencias del sistema y las herramientas necesarias.
# Algunas versiones son específicas y pueden requerir métodos de instalación especiales (como nvm para Node.js).

# Paquetes base del sistema (instalables con apt-get)
ca-certificates
curl
git
build-essential
python3
python3-pip
libssl-dev
pkg-config

# Dependencias de la base de datos (SQLite)
sqlite3
libsqlite3-dev

# Ecosistema de Node.js (se recomienda usar nvm para gestionar las versiones)
# Instalar nvm: curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.1/install.sh | bash
# Luego, usar nvm para instalar la versión correcta:
# nvm install 20.17.0
# nvm use 20.17.0
nodejs==20.17.0
npm==10.8.0

# Gestor de procesos para producción (instalable con npm)
# npm install -g pm2@5.4.0
pm2==5.4.0
